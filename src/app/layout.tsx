import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Manevia | Premium Wig Discovery & Virtual Try-On',
  description: 'The world\'s No.1 human hair wig brand. Shop premium human hair & glueless wigs with our AI Virtual Try-On Studio.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
